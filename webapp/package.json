{
  "name": "frameforge-webapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "setup": "npm install && npm run db:generate && npm run db:push"
  },
  "dependencies": {
    "archiver": "^6.0.2",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "prisma": "^7.2.0",
    "unzipper": "^0.10.14",
    "@prisma/client": "^5.16.2"
  }
}
